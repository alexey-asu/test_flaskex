В коде скрипта forms.py, использующего модуль wtforms используются валидаторы проверки заполнения полей username и password.
При этом в конструкции описания валидаторов допущена синтаксическая ошибка. Указан несуществующий класс required().
Класс required() заменен на InputRequired()
